# Admin menu v0.8.0

`AdminKitMenuBuilder` builds Bitrix admin menu arrays from registered resources and standalone pages. It sorts items by `sort()`, groups by `group()` / `getParentMenuId()`, includes icons, supports nested `items`, and skips records hidden by `isVisibleInMenu()` or `canView()`.
