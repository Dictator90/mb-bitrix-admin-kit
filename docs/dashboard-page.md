# DashboardPage v0.8.0

`DashboardPage` is a lightweight `CustomPage` specialization. Override `widgets()` to return widget HTML blocks; AdminKit wraps them in `adminkit-dashboard` and `adminkit-dashboard__widget` containers.
