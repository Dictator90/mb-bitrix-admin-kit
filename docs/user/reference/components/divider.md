# Divider

Класс: `MB\Bitrix\AdminKit\Component\Layout\Divider`.

Назначение: горизонтальный разделитель (с подписью или без).

Методы:
- `make(string $label = '')`

Пример:
```php
Divider::make();
Divider::make('Или');
```
