# LineBreak

Класс: `MB\Bitrix\AdminKit\Component\Layout\LineBreak`.

Назначение: вертикальный отступ фиксированной высоты.

Методы:
- `make(int $height = 16)`

Пример:
```php
LineBreak::make();
LineBreak::make(24);
```
